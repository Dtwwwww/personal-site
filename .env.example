# Cloudflare Pages 本地开发环境变量
# 使用方法：
#   1. 复制此文件为 .dev.vars
#   2. 将 sk-xxx 替换为你的真实 DeepSeek API Key（platform.deepseek.com 注册获取）
#   3. 运行: wrangler pages dev
# .dev.vars 已在 .gitignore 中忽略，不会被提交
#
# 生产环境：使用 wrangler pages secret put DEEPSEEK_API_KEY 设置
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
